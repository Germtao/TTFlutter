import 'package:flutter/material.dart';

class PhotoViewPage extends StatelessWidget {
  static const className = 'PhotoViewPage';

  PhotoViewPage();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
